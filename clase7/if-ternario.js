const estado = 200; //luego cambiar el valor a 200
let mensaje = ' ';

if (estado === 200){
mensaje = 'Ok!';
} else {
    mensaje = 'Error';
}

//const mensaje = (estado === 200) ? "Ok!" : "Error!"

console.log(mensaje);